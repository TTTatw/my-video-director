---
name: my-video-director
description: "导演级视频脚本生成器。拿一张产品图片(可选附需求) → 输出分析(产品画像、Big Idea、角色卡(单人/多人,含长相+穿搭+人设)、逐镜头分镜(镜头规格+电影工艺)、表演层(objective/beats/subtext/eye-life)、口播类型)＋**必需结尾的最终可执行生成提示词(按用户提到的模型自动切换方言：Seedance 2.5 / Seedance 2.0 / Veo 3/3.1；默认 Seedance 2.5)**。分析普适，⑦按模型方言输出。Use when: 用户给产品图片想生成有剧情的视频脚本、要有分镜/角色/表演描述、或只想快速套用 UGC 模板出片，并且要一段能直接拿去生成的最终提示词。"
user-invocable: true
metadata:
  tags: [video, director, storyboard, character, acting, ugc, script, product, markdown]
  version: 1.0.0
  updated: 2026-08-28
---

# My Video Director — 导演级视频脚本生成器

一个**创意/导演层** skill：它不执行生成，而是从一张产品图片出发，产出一份**完整、可直接执行的『拍摄案』**。

> 定位：自动补上开源 Higgsfield skills 缺的那一层——它们都是「执行层」(怎么选模型、怎么填模板、怎么调 CLI)，而这个 skill 是「**导演层**」：给产品 → 人物 + 分镜 + 表演 + 口播类型。**你拿到它输出的文本，丢给任何视频生成模型都能用。**

核心产出（一次调用，①~⑥ 分析 + **⑦ 最终可执行提示词〔必需〕**）：

```
① 产品画像     —— 品类 / 核心卖点 / 目标人群 / 调性 / 痛点 / 使用场景
② Big Idea 概念 —— 一句话让人记住的主张 + 2~3 个可执行的创意方向
③ 角色卡(单人/多人) —— 每个角色：长相 / 穿搭 / 人设 / 是否发声 / 与产品关系
④ 分镜表       —— 逐镜头：编号 / 时长 / 机位景别 / 画面内容(状态而非过程)
⑤ 表演层       —— 逐镜头：每个角色的 objective / beats / subtext / 肢体任务 / 眼神
⑥ 口播类型     —— 有声口播 / 无口播+字幕 / 背景配音 / ASMR 静音
⑦ 最终提示词〔必需〕—— 按 Seedance 2.5 规范合成的可复制生成提示词
```

> **⑦ 是必须交付**。①~⑥ 是支撑它的分析，可以同时给出供你审；但**不管怎样，结尾必须给一段可直接复制去生成的最终提示词**（`skills/final-prompt/` 负责产出）。

---

## 什么时候被触发

- 用户**给了一张产品图片**，想要**视频脚本 / 剧情 / 分镜 / 角色设定**。
- 用户想从产品生成**有创意、像编导做出来**的脚本，而非套模板。
- 用户**只想要一条 UGC 视频**，希望直接套模板快速出脚本。

## 两条路径（都是全品质，无"精简"）

根路由器根据用户意图决定**起点**（判定表见 `references/routing.md`），但**两条路径都不降品质**，都产出完整 ①~⑦，⑦都是最终 Seedance 2.5 提示词。差别只在"创意从哪来"——模板路径从预置格式+概念种子起，导演路径从洞察+big idea 起。

| 路径 | 适用 | 流程 |
|---|---|---|
| **模板路径 (template)** | "我就要一条 UGC / 想要某个确定格式" | 选模板 → 填概念种子 → **仍走完整 ①~⑦** |
| **导演路径 (creative)** | "要剧情 / 要有创意 / 像真人编导" | ①→⑦ 全流程 |

> **没有"精简版/广告版"这一档。** 表演层永远做满（master profile + 逐场改写段落 + eye-life），最终提示词永远按**用户提到的模型对应方言**做满（Seedance 2.5 / 2.0 / Veo 3.1，默认 2.5），绝不降级。

## 输入

1. **必须**：一张产品图片（或多个角度）。
2. **可选**：一句话需求，例如：
   - "做成开箱 UGC，单人、带口播"
   - "要两个角色对话，一个介绍产品一个试用，背景配音"
   - "只给脚本，口播，不要字幕"
3. 可选补充：目标人群、调性、时长、平台（抖音/TikTok/IG/YT 都可，不影响结构；若要最终 prompt 可另说）。

### 三个必须先锁定的输入（缺两个就先问）

| 输入 | 为什么它决定成败 | 没给时怎么办 |
|---|---|---|
| **目标市场 / 口播语言** | 台词、屏幕字、字幕、caption 全跟着它走；美国市场配中文台词＝交付物直接作废 | 先问一句；不答就默认中国普通话，并在**回答开头**写明这个默认 |
| **平台 + 时长** | 决定静音可读性、CTA 写法（`link in bio` 在 YouTube/Google 上不成立）、镜头数量 | 先问；不答按时长给最保守版本，并说明取舍 |
| **场景 / 环境** | 决定本条片子的空间与光源，不能沿用上一条 | 先问；不答就自己选，并说明"本条场景与上一条不同" |

> **同一个产品 ≠ 同一条片子。** 出第二条时场景、光源锚点、开场动作都要重新选，并在回答里点明与上一条的差别。

## 使用规则

1. **先出交付物，不讲解内部机制**。不叙事"正在做产品画像""正在生成角色"——用阶段小标题带过即可。
2. **语言分层：先看清读者是谁，再决定用什么语言写。** 交付物里有三种读者，别混：
   - ①~⑥ 分析层 → **用户的语言**（默认中文），保留 objective / beats / subtext / eye-life / status 等英文专业词；
   - 台词、屏幕字、字幕、caption → **目标市场的母语**。投美国就是自然美式英语，投日本就是日语；**用户用中文跟你说话，不代表台词写中文**；
   - ⑦ 提示词正文 → **模型方言**（见 `references/model-dialects.md`，Seedance / Veo 规范本身以英文为主）。
   目标市场没给就先问；要默认，就在回答开头写"默认：中文台词"。
3. **不虚构产品卖点**。图片看不出的 claim 标注 `[待确认]`，别编。
4. **状态，不是过程**。分镜里写"手臂前伸、已扔出"，不写"伸手抓住、然后扔出去"。
5. **年龄用描述不用数字**。不写"25 岁"，写"一张已被生活磨粗的脸""旧拳手的气场"。
6. **普适、不分平台**。输出的分镜/角色/口播口径是通用的，可丢给任何视频模型。
7. **场景与光源不继承**。上一条用过窗边、卧室、同一种自然光，下一条就换空间或换锚点，并说明换了什么；同一批次里两条片子撞场景＝看起来像同一条素材剪的。
8. **别用格式完整度掩盖决策深度**。①~⑦ 全给齐不代表写对了：场景、光源锚点、开场动作、第一句话、CTA 写法，每一个微观决定都要能说出"为什么是这个，而不是别的"。
9. **选角要给出依据，不许用默认值。** 角色的族裔、年龄感、体型由**目标买家的自我镜像**决定，不由卖家出身、产品品类、上一条用过的创作者、或"哪个好看"决定。产品只决定角色的功能位（谁用、什么场合、什么调性、做什么动作）。每张角色卡的第一行必须是**「选角依据」**，写法见 `skills/character/`。

## 调用方式

直接给产品图片（+ 需求）。如果需要单独细化某一步，也可以直接点名子技能：

- 只要角色 → 读 `skills/character/`
- 只要分镜 → 读 `skills/storyboard/`
- 只要表演层 → 读 `skills/acting/`
- 只要口播类型 → 读 `skills/voice/`
- 要 UGC 模板 → 读 `skills/templates/`
- **要最终生成提示词 → 读 `skills/final-prompt/`**（Seedance 2.5 规范）

## 最终交付格式（分析 ①~⑥ + 必需 ⑦）

输出严格按下面顺序排，每段一个小标题。**⑦ 是必需终点**。附上 `examples/` 里的完整示例（含 ⑦ 最终提示词），可直接对照。

```markdown
## ① 产品画像
品类 / 核心卖点 / 目标人群 / 调性色板 / 人群痛点 / 使用场景 / 语言风格

## ② Big Idea
一句话主张 + 2~3 个创意方向（每个含：一句话 concept、视频类型、节奏、记忆点）

## ③ 角色卡
单人 → 一张卡；多人 → 每人一张卡
每卡第一行：**选角依据**（买家自我镜像 → 本品的身份符号 → 所以选这个人的族裔/年龄感/体型/气质）
每卡其余：长相 / 穿搭 / 人设 / 是否发声 / 与产品关系 / 场景里的定位

## ④ 分镜表
逐镜头，每镜给**镜头规格**（镜头描述在前、人物在后）：景别 / 焦段 / 机位角度 / 景深 / 运镜 / 纵深机制(三选一) / 光源动机 / 构图 + 画面内容(状态) / 人物在做什么 / 转场
> 用 `skills/storyboard/` 的镜头工艺规则写，避免"默认平视+固定"和 AI 廉价感

## ⑤ 表演层
逐镜头：每个在画角色 → **master profile(150–220 词单段) + 逐场改写段落** + objective/beats/subtext/肢体任务/眼神 + 0–5 自检(≥4)
> 不允许只给压缩 bullet。这是"导演级"与"模板感"的分界线。

## ⑥ 口播类型
有声口播 → 说话人 + 声线 + 台词
无口播+字幕 → 字幕文案(后期上加)
背景配音 → 视觉 + 背景 VO(说话人/声线/内容)
ASMR 静音 → 无声、字幕或 caption-only

## ⑦ 最终提示词〔必需〕
按 Seedance 2.5 规范合成的一段**可直接复制**的生成提示词（核心公式 + 参考物角色/排除 + 括号语法 + 首末帧(如用) + 分段 end state + 真人 7 槽），**另起一行**给生成参数(mode/resolution/duration/aspect_ratio)。
> 详见 `skills/final-prompt/`。
```

---

## 交付前自检（逐条过，不过就改；用户要验收清单时可附在结尾）

- [ ] 台词 / 屏幕字 / 字幕 / caption 的语言 = **目标市场语言**？（投美国却出现中文台词＝不合格）
- [ ] 每张角色卡都有**「选角依据」**那一行？族裔/年龄感/体型是按买家自我镜像选的，不是沿用上一条、也不是品类刻板印象？
- [ ] 本条的场景与光源锚点是**重新选的**，且与上一条不同？
- [ ] 全片每个镜头的光源**锚点不是同一个**？（全片只有窗光＝光太平）
- [ ] 每个镜头的光都写了**具名来源 + 方向 + 衰减**，没有"环境光""侧光"这种无来源的光？
- [ ] 第一句话 / 第一个画面是具体的东西（名词、数字、动作），不是"今天聊聊为什么 X"？
- [ ] 台词按目标语言的正常语速念一遍，时长够不够？
- [ ] 产品卖点有没有凭空发明？（材质、保暖、显瘦、价格没依据就标 `[待确认]`）
- [ ] ⑦ 里参考物的 role map、保真度、排除项都写全了？

---

## 相关子技能

- `skills/product-intake/` — 产品画像
- `skills/treatment/` — 创意层/big idea/视频类型选择
- `skills/character/` — 单人/多人角色卡
- `skills/storyboard/` — 分镜
- `skills/acting/` — 表演层
- `skills/voice/` — 口播类型字典
- `skills/templates/` — 10 种 UGC 模板 + 概念种子（**必须配齐短视频工艺**）
- `skills/final-prompt/` — **最终可执行提示词（Seedance 2.5 规范）〔必需终点〕**
- `references/routing.md` — 路径判定表 + 视频类型字典 + 角色数判定
- `references/short-video-craft.md` — **短视频工艺层**：3 秒钩子原型 / 留人结构 / 心理杠杆 / 反转结构 / 服装美妆心理
